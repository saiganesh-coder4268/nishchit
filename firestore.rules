rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open permissive rules for Nishchit transport operations, driver marketplace, and live fleet telemetry
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
